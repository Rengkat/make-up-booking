import { createSlice } from "@reduxjs/toolkit";

const initialState = {};
const AppSlice = createSlice({
  name: "App",
  initialState,
  reducers: {},
  extraReducers: {},
});

export default AppSlice.reducer;
